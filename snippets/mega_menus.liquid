{%- assign hasMegaMenu = false -%}
{%- if settings.mega_menu_1 -%}
	{%- section 'mega_menu_1' -%}
	{%- assign hasMegaMenu = true -%}
{%- endif -%}
{%- if settings.mega_menu_2 -%}
	{%- section 'mega_menu_2' -%}
	{%- assign hasMegaMenu = true -%}
{%- endif -%}
{%- if settings.mega_menu_3 -%}
	{%- section 'mega_menu_3' -%}
	{%- assign hasMegaMenu = true -%}
{%- endif -%}
{%- if settings.mega_menu_4 -%}
	{%- section 'mega_menu_4' -%}
	{%- assign hasMegaMenu = true -%}
{%- endif -%}
{%- if settings.mega_menu_5 -%}
	{%- section 'mega_menu_5' -%}
	{%- assign hasMegaMenu = true -%}
{%- endif -%}
{%- if settings.mega_menu_6 -%}
	{%- section 'mega_menu_6' -%}
	{%- assign hasMegaMenu = true -%}
{%- endif -%}

{%- section 'mega_menu_mobile' -%}

{%- if hasMegaMenu -%}
<style>
  .mega-menu {display: none; box-shadow: 0em 1.5em 1.5em rgba(0,0,0,0.3); border-top: 1px solid #31443842; position: absolute; top: 100%; left: 0 !important; width: 100%; z-index: 1000; font-size: 1rem; text-transform: uppercase; font-weight: {{ settings.body_font_weight }}; font-style: {{ settings.body_font_style }}; letter-spacing: {{settings.body_font_letter_spacing}};}
  .mega-menu .container {padding: 0;}
  .mega-menu * {text-transform: none; }
  .mega-menu p {margin: 0;}
  .mega-menu img {display: block; max-width: 100%;} 
  .mega-menu a {display: block !important; padding: 0.1em 0 !important;}
  .mega-menu .mega-menu-content {display: -ms-flexbox; display: flex; display: -webkit-box; display: -webkit-flex; justify-content: center; width: 100%; margin: 0 auto; }
  .mega-menu .column {display: inline-block;}
  .mega-menu .column > div {margin-bottom: 1.5em;}
  .mega-menu .column > div:last-child {margin: 0;}
  .mega-menu .linklist-title { {% if settings.heading_font_custom != "" %}{{settings.heading_font_custom_name}}{% else %}font-family: {{settings.heading_font.family}}, {{settings.heading_font.fallback_families}};{% endif %}; font-size: 2em; margin-top: 0.2em; margin-bottom: 0.5em;} 
  .mega-menu .linklist-title a {padding: 0 !important;}
  .mega-menu .link {line-height: 180%;}
  .mega-menu .img-caption {margin-top: 1em; font-weight: 500;}
  .mega-menu .info {margin-top: 1em; position: relative; padding-right: 6em;}
  .mega-menu .prod-title {font-weight: 700; text-align: left;}
  .mega-menu .prod-price {position: absolute; top: 0; right: 0;}
  .mega-menu .btn {margin-top: 1em; white-space: nowrap;}
  {%- if settings.mega_menu_width == "full" -%}
  @media all and (min-width: 901px){
    .header .main-content {position: static;}
  }
  .mega-menu .mega-menu-content {max-width: 1440px; margin: auto;}
  {%- endif -%}
  {%- if settings.mega_menu_width == "container" or settings.mega_menu_width == "adjust" -%}
  .mega-menu {background: transparent !important;}
  .mega-menu .mega-menu-content {max-width: {{settings.container_width}}; margin: auto;}
  {%- endif -%}
  {%- if settings.mega_menu_width == "adjust" and settings.mega_menu_position == "parent" -%}
  .mega-menu {min-width: 40em; padding: 0;}
  .mega-menu .column {width: auto !important;}
  {%- else -%}
  .mega-menu-trigger {position: static !important;}
  {%- endif -%}

  .mega-menu-content .hide {
    display: none;
  }

  @media all and (max-width: 1130px){
    .main-menu .submenu.mega-menu {font-size: 1.24vw;}
  }
</style>
{%- endif -%}